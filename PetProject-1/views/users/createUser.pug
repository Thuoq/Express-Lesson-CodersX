extends ../layout

block content
	h2 Please Type User
	if name 
		.alert.alert-danger= "Name Too Long Please Type Again"
	form(action="/users/create",method="POST")
		input(type="text",name="name", value = name ? name: "",placeholder="Type Nick name User")
		br
		span &nbsp;
		br
		button(class="btn btn-primary") Add User
